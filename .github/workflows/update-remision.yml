name: 🔎 Validar secuencia e historial
run-name: >
  ${{ github.event_name == 'workflow_dispatch' && '🔄 Ejecución manual' ||
      '🚀 Actualización automática por archivos modificados' }}

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'data/secuencia.json'
      - 'data/historial.json'

permissions:
  contents: write

jobs:
  validate-json:
    runs-on: ubuntu-latest

    steps:
    - name: 📦 Checkout repo
      uses: actions/checkout@v4

    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: 🧪 Validar archivos JSON
      run: |
        echo "Validando data/secuencia.json..."
        cat data/secuencia.json | jq empty
        echo "✅ secuencia.json es válido"

        echo "Validando data/historial.json..."
        cat data/historial.json | jq empty
        echo "✅ historial.json es válido"

    - name: 💾 Commit si es necesario
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/secuencia.json data/historial.json
        git diff --quiet && git diff --staged --quiet || (
          git commit -m "🤖 Validación automática de archivos de remisión" &&
          git push
        )
