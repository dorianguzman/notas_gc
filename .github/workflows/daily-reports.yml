name: 📊 Generate Sales Reports
run-name: ${{ github.event_name == 'workflow_dispatch' && '🔄 Manual report generation' || github.event_name == 'schedule' && '📊 Scheduled daily report generation' || '📊 Report generation' }}

on:
  # Run daily at 1 AM (Mexico City time is UTC-6, so 7 AM UTC)
  schedule:
    - cron: '0 7 * * *'

  # Allow manual trigger
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-reports:
    name: ${{ github.event_name == 'workflow_dispatch' && '🔄 Manual report generation' || github.event_name == 'schedule' && '📊 Scheduled report generation' || '📊 Report generation' }}
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          pip install gspread google-auth

      - name: 📊 Generate sales reports
        env:
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
        run: |
          python scripts/generate_reports.py

      - name: 💾 Commit and push data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # Add generated data files (create data/ folder if it doesn't exist)
          mkdir -p data
          git add data/ || echo "No data to add"

          # Check if there are changes to commit
          if git diff --staged --quiet; then
            echo "✅ No changes to commit"
          else
            git commit -m "📊 Update sales data - $(date +'%Y-%m-%d')"
            git push
            echo "✅ Data updated and pushed"
          fi
